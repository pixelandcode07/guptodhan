import SectionTitle from '@/Components/SectionTitle';
import React from 'react';

const page = () => {
  return (
    <div>
      <SectionTitle text="Test modeule" />
    </div>
  );
};

export default page;
